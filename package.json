{
  "name": "shenpi",
  "version": "1.0.0",
  "description": "买不买 - 购买审批神器",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "express": "^4.21.0",
    "uuid": "^10.0.0"
  }
}
